---
navigation:
  heading: Home
  order: 10
topics:
  opener:
    heading: Jacco Meijer
    abstract: Senior IT consultant, specialized in Web Applications and Cyber Security. Based in Utrecht, The Netherlands.
    image: topicImage
  blog:
    heading: Latest Blog posts
    abstract: On web applications and cyber security.
    variant: headline

---
export { WideLayout as default } from '../../layout/wide-layout.jsx'
import { Topic, TopicList } from '@jaccomeijer/green-lib'
import topicImage from './face.png'
import { globals } from '../../config/globals.js'
import { getOpenerTopicByNavigationId, getOpenerTopics } from '../blog/get-opener-topics.js'
import { blogImages } from '../blog/blog-images.js'

<Topic
  variant="profile"
  globals={globals}
  images={{topicImage}}
  topic={frontmatter.topics.opener}
  heading-element="h1"
/>

---

<Topic
  variant="profile"
  globals={globals}
  images={{topicImage}}
  topic={frontmatter.topics.blog}
/>

<TopicList
  className="s-grid u-grid-three-cols u-gap-m"
  variant="card"
  globals={globals}
  images={blogImages}
  topics={getOpenerTopics({ pages: props.pages, tag: 'blog' }).slice(0,3) }
/>
