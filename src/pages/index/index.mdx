---
navigation:
  heading: Home
  order: 10
topics:
  opener:
    heading: Jacco Meijer
    abstract: Cyber Security Professional based in Utrecht, The Netherlands.
    image: topicImage
---
export { TextLayout as default } from '../../layout/text-layout.jsx'
import { Topic, TopicList } from '@jaccomeijer/green-lib'
import topicImage from './face.png'
import { globals } from '../../config/globals.js'
import { getOpenerTopicByNavigationId } from '../blog/get-opener-topics.js'

<div class="s-popout u-inline-gutter">
  <Topic
    data-variant="profile"
    globals={globals}
    images={{topicImage}}
    topic={frontmatter.topics.opener}
  />
</div>

---

<Topic
  data-variant="profile"
  globals={globals}
  topic={getOpenerTopicByNavigationId({ pages: props.pages, navigationId: 'blog' })}
/>
