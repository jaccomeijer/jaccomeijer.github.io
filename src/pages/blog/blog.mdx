---
navigation:
  id: blog
  heading: Blog
  order: 20
topics:
  opener:
    heading: Blog
    abstract: On web applications and cyber security.
    icon: message-circle
    variant: headline
---
export { WideLayout as default } from '../../layout/wide-layout.jsx'
import { getOpenerTopics } from './get-opener-topics.js'
import { globals } from '../../config/globals.js'
import { blogImages } from './blog-images.js'
import { Topic, TopicList } from '@jaccomeijer/green-lib'

<div className="s-popout">
  <Topic
    variant="headline"
    globals={globals}
    topic={frontmatter.topics.opener}
  />
</div>

---

<TopicList
  className="s-grid u-grid-three-cols u-gap-m"
  variant="card"
  globals={globals}
  images={blogImages}
  topics={getOpenerTopics({ pages: props.pages, tag: 'blog' })}
/>
