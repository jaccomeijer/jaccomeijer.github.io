---
navigation:
  heading: Topic Profile
  parent: Topic
topic:
  heading: Profile example topic
  labels: [opinion, test, demo, story]
  abstract: This is an example of a topic abstract text.
  image: /green-lib/story-assets/images/pexels-skyler-ewing-4596712.jpg
  icon: share
  action:
    heading: Test action
    icon: archive
---

export { Layout as default } from '../../story-layout/layout'
import { Topic } from './topic'

## {props.navigation.heading}

### Example

```html
<Topic topic={props.topic} variant="profile" />
```

<Topic topic={props.topic} variant="profile" />

### Example

```html
<Topic topic={props.topic} variant="profile" swapImage={true} />
```

<Topic topic={props.topic} variant="profile" swapImage={true} />

### Example

```html
<Topic topic={{...props.topic}} variant="profile" />
```

<Topic topic={{ ...props.topic }} variant="profile" />

### Example

```html
<div className="s-popout">
  <Topic topic={props.topic} variant="profile" headingElement="h1" />
</div>
```

<div className="s-popout">
  <Topic topic={props.topic} variant="profile" headingElement="h1" />
</div>

### Frontmatter

```yaml
topic:
  heading: Profile example topic
  labels: [opinion, test, demo, story]
  abstract: This is an example of a topic abstract text.
  image: /green-lib/story-assets/images/pexels-skyler-ewing-4596712.jpg
  icon: share
  action:
    heading: Test action
    icon: archive
```
